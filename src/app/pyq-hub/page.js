import React from 'react';
import PyqHub from '../../views/PyqHub';

export const metadata = {
  title: 'PYQ Hub — Previous Year Questions | TCE - The Competitive Edge',
  description: 'Practice real previous year question papers for WBP, KP SI, SSC GD and Railway (RRB). 100% free, no batch enrollment required.',
  alternates: { canonical: '/pyq-hub' },
};

export default function Page() {
  return <PyqHub />;
}
